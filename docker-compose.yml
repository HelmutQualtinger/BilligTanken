services:
  billigtanken:
    build: .
    container_name: billigtanken
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - logs:/var/log
    networks:
      - default
      - reverse-proxy

volumes:
  logs:

networks:
  reverse-proxy:
    external: true
